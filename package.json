{
  "name": "agentic-layer-docs",
  "version": "1.0.0",
  "description": "Agentic Layer Documentation",
  "scripts": {
    "build": "antora generate --log-failure-level warn antora-playbook.yml",
    "fetch": "antora generate --fetch antora-playbook.yml"
  },
  "devDependencies": {
    "@antora/cli": "^3.1.12",
    "@antora/site-generator": "^3.1.12",
    "asciidoctor-kroki": "^0.18.1"
  }
}
